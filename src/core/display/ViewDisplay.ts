export class ViewDisplay {

    static format(
        view?: string
    ): string {

        if (

            !view

        ) {

            return "View Not Available";

        }

        return view;

    }

}