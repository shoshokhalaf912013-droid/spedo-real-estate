export function createSlug(
  projectName: string,
  unitNumber: string
): string {

  return `${projectName}-${unitNumber}`
    .trim()
    .toLowerCase()
    .replace(/[^a-z0-9\u0600-\u06ff]+/g, "-")
    .replace(/^-+|-+$/g, "");

}